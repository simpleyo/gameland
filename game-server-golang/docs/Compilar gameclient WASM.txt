Para ver las variables de entorno de go:
    go env

Para lista las arquitecturas y los OS disponibles para cross-compiling:
    go tool dist list 

Para seleccionar OS y ARCH:
    set GOOS=js
    set GOARCH=wasm

Para compilar WASM:
    go build -o ./gameclient/wasm/main.wasm ./gameclient

