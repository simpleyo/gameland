//go:build example
// +build example

package main

import (
	"fmt"
	"game-server-golang/tank"
)

// Execute: go run -tags=example game-server-golang
func main() {
	tk := tank.Tank{}
	tksd := tank.TankSnapData{}
	tk.InitEntity(0)
	tk.S.Init(&tksd)

	fmt.Println("Example")

	// c := tk.S.Curr()

	// tank.Entity.EntityOwner = &tank

	// tank.Entity.OnMarkedToBeDeleted()

}
